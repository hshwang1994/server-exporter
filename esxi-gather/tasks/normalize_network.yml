---
# esxi-gather/tasks/normalize_network.yml
# _e_raw_facts + _e_raw_config → network fragment

- name: "esxi | normalize network | extract dns and gw"
  ansible.builtin.set_fact:
    _e_default_gw:  "{{ _e_raw_facts.ansible_default_ipv4.gateway | default(none) }}"
    # 잔류 fix (Round 12 후속): vmware_host_dns_info (collect_dns.yml) 결과를 1순위로,
    # 기존 vmware_host_config_info drill-in 을 폴백으로. dns_info 모듈은 host_name 키로
    # ip_address (DNS 서버 list) / search_domain / domain_name 정확 반환.
    _e_dns_servers: >-
      {%- set dns_out = [] -%}
      {%- set dns_dict = _e_raw_dns | default({}) -%}
      {%- if dns_dict is mapping and (dns_dict | length > 0) -%}
        {%- set first_key = (dns_dict.keys() | list | first) -%}
        {%- set entry = dns_dict.get(first_key, {}) -%}
        {%- for srv in (entry.ip_address | default([])) -%}
          {%- if srv -%}{%- set _ = dns_out.append(srv | string) -%}{%- endif -%}
        {%- endfor -%}
      {%- endif -%}
      {%- if not dns_out -%}
        {# fallback: vmware_host_config_info drill-in (production-audit 보존) #}
        {%- set host_cfgs = (_e_raw_config | default({})).values() | list -%}
        {%- for host_cfg in host_cfgs -%}
          {%- set net_info = host_cfg.get('network_system_info', host_cfg) if host_cfg is mapping else {} -%}
          {%- set dns_cfg = (net_info.get('dns_config') if net_info is mapping else {}) | default({}) -%}
          {%- for srv in (dns_cfg.address | default([])) -%}
            {%- if srv -%}{%- set _ = dns_out.append(srv | string) -%}{%- endif -%}
          {%- endfor -%}
        {%- endfor -%}
      {%- endif -%}
      {{ dns_out | unique | list }}
    _e_search_domains: >-
      {%- set out = [] -%}
      {%- set dns_dict = _e_raw_dns | default({}) -%}
      {%- if dns_dict is mapping and (dns_dict | length > 0) -%}
        {%- set first_key = (dns_dict.keys() | list | first) -%}
        {%- set entry = dns_dict.get(first_key, {}) -%}
        {%- for sd in (entry.search_domain | default([])) -%}
          {%- if sd -%}{%- set _ = out.append(sd | string) -%}{%- endif -%}
        {%- endfor -%}
      {%- endif -%}
      {{ out }}
  no_log: true

- name: "esxi | normalize network | build interfaces"
  ansible.builtin.set_fact:
    _e_norm_interfaces: >-
      {%- set out = [] -%}
      {%- set primary = _e_raw_facts.ansible_default_ipv4.interface | default('') -%}
      {%- for iface in (_e_raw_facts.ansible_interfaces | default([])) -%}
        {%- set d    = _e_raw_facts.get('ansible_' ~ iface, {}) -%}
        {%- set ipv4 = d.get('ipv4', {}) -%}
        {%- if ipv4.get('address') and
               ipv4.address != '127.0.0.1' and
               not ipv4.address.startswith('169.254.') -%}
          {%- set nm = ipv4.netmask | default('') -%}
          {# production-audit (2026-04-29): bit-counting (Linux 와 일관) — /22, /26, /28 등 비표준 prefix 지원 #}
          {%- set pfx = none -%}
          {%- if nm -%}
            {# cycle 2026-05-07-post fix: val 을 namespace 에 포함 — 이전에는 inner for(bit) 안 set val 이 outer scope 미반영 → /23, /30 같은 비표준 mask 잘못 계산. /24 만 우연히 정상. #}
            {%- set pns = namespace(bits=0, val=0) -%}
            {%- for octet in nm.split('.') -%}
              {%- set pns.val = octet | int -%}
              {%- for bit in [128,64,32,16,8,4,2,1] -%}
                {%- if pns.val >= bit -%}
                  {%- set pns.bits = pns.bits + 1 -%}
                  {%- set pns.val = pns.val - bit -%}
                {%- endif -%}
              {%- endfor -%}
            {%- endfor -%}
            {%- set pfx = pns.bits -%}
          {%- endif -%}
          {%- set is_p = (iface == primary) -%}
          {%- set _ = out.append({
            'id':          iface, 'name': iface, 'kind': 'vmkernel',
            'mac':         d.get('macaddress',none),
            'mtu':         d.get('mtu',none),
            'speed_mbps':  none,
            'link_status': 'up' if d.get('active',false) else 'unknown',
            'is_primary':  is_p,
            'addresses': [{'family':'ipv4','address':ipv4.address,
                           'prefix_length':pfx,
                           'subnet_mask': nm if nm else none,
                           'gateway': _e_default_gw if is_p else none}]
          }) -%}
        {%- endif -%}
      {%- endfor -%}
      {{ out }}
  no_log: true

# cycle-016 (2026-04-29): ESXi network.summary
# 2026-06-30: vmk 인터페이스는 speed_mbps=none(line 79) 이라 여기서 group 산출 시 'null qty' 누출됨
#   (site 10.100.64.2 실측: summary=[{speed:null,qty:1}]). 따라서 (1) null/0 speed 가드 추가로
#   vmk 기반 누출 차단(→ groups=[]), (2) 실제 NIC 속도 summary 는 collect_network_extended 에서
#   pnic(_e_ext_adapters, 실 speed 보유) 기준으로 산출해 merge 시 덮어쓴다 (Windows/Linux 동일 패턴).
- name: "esxi | normalize network | compute summary groups"
  ansible.builtin.set_fact:
    _e_net_summary: >-
      {%- set ns = namespace(groups=[], seen={}) -%}
      {%- for n in (_e_norm_interfaces | default([])) -%}
        {%- set sp = n.speed_mbps | default(none) -%}
        {%- if sp is not none and (sp | int) > 0 -%}
          {%- set link_up = (n.link_status | default('') | lower) in ['up','linkup'] -%}
          {%- set key = sp | string -%}
          {%- if key in ns.seen -%}
            {%- set idx = ns.seen[key] -%}
            {%- set _ = ns.groups[idx].update({'quantity': ns.groups[idx].quantity + 1, 'link_up_count': ns.groups[idx].link_up_count + (1 if link_up else 0)}) -%}
          {%- else -%}
            {%- set _ = ns.seen.update({key: ns.groups | length}) -%}
            {%- set _ = ns.groups.append({'speed_mbps': sp, 'link_type': none, 'quantity': 1, 'link_up_count': 1 if link_up else 0}) -%}
          {%- endif -%}
        {%- endif -%}
      {%- endfor -%}
      {{ {'groups': ns.groups} }}
  no_log: true

# cycle-016 Phase P: 3 채널 key 일관성 — adapters / ports / driver_map 빈 list 키 노출
- name: "esxi | normalize network | build fragment"
  ansible.builtin.set_fact:
    _data_fragment:
      network:
        dns_servers: "{{ _e_dns_servers }}"
        default_gateways: >-
          {%- set out = [] -%}
          {%- if _e_default_gw -%}
            {%- set _ = out.append({'family':'ipv4','address':_e_default_gw}) -%}
          {%- endif -%}
          {{ out }}
        interfaces: "{{ _e_norm_interfaces }}"
        adapters:    []
        ports:       []
        driver_map:  []
        summary: "{{ _e_net_summary | default({'groups': []}) }}"
    _sections_supported_fragment: ['network']
    _sections_collected_fragment: ['network']
    _sections_failed_fragment:    []
    # 2026-08-12 (N36-c): _e_dns_ok / _e_config_ok 배선. 종전에는 두 플래그 모두 소비처가
    #   0건이라 이름 서버 수집이 실패해도 흔적이 남지 않았다.
    #
    #   이름 서버는 2경로다 — dns_info(1순위) → config_info(폴백, 위 :21-31).
    #   한쪽이 실패해도 다른 쪽이 값을 채웠으면 그것은 **성공한 fallback** 이므로 error 가
    #   아니다. 둘 다 성공했는데 결과가 비었다면 실제로 설정이 없는 것이라 역시 error 가
    #   아니다. 결과가 비었는데 두 경로 중 하나라도 실패한 경우에만 "미설정" 과 "수집 실패"
    #   를 구분할 수 없으므로 error 로 승격한다.
    #   섹션 status 는 바꾸지 않는다 (network 는 인터페이스까지 정상 수집된 상태다).
    _errors_fragment: >-
      {{ [{'section': 'network',
           'message': '네트워크 정보 중 일부를 수집하지 못했습니다. 대상 상태와 수집 로그를 확인하세요.',
           'detail':  'dns_servers=[] (dns_info ok=' ~ (_e_dns_ok | default(false)) ~
                      ', config_info ok=' ~ (_e_config_ok | default(false)) ~ ')'}]
         if (((_e_dns_servers | default([]) | length) == 0)
             and not ((_e_dns_ok | default(false) | bool)
                      and (_e_config_ok | default(false) | bool)))
         else [] }}
  no_log: true

- name: "esxi | normalize network | merge fragment"
  ansible.builtin.include_tasks:
    file: "{{ lookup('env','REPO_ROOT') }}/common/tasks/normalize/merge_fragment.yml"
